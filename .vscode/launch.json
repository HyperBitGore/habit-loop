{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug Go server",
      "type": "go",
      "request": "launch",
      "mode": "debug",
      "program": "${workspaceFolder}/server",
      "cwd": "${workspaceFolder}/server",
      "preLaunchTask": "go: build server"
    }
  ]
}
